"""Workflow engine package.

Self-contained workflow engine built on LangGraph. Keeps import cheap:
this module defines only ``__version__`` and imports no dependencies.
"""

__version__: str = "0.1.0"
