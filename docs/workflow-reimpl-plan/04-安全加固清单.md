# 04 · 安全加固清单（spec-09 TC1）

> 验证基准：`spec/spec-09-加固与交付.md` TC1、`spec/CONTRACT.md` §10 G7、
> `03-隐患修复方案.md` H4/H5/H6。验证日期：2026-08-11。全部项目已勾选，
> 无 CRITICAL/HIGH 遗留。

## 一、grep 三检

### ① 硬编码密钥扫描

命令：

```bash
grep -rniE "(api[_-]?key|token|secret|password)\s*[:=]" app/workflow/ tests/
```

逐条人工判定（全部为误报，无真实密钥）：

| 命中位置 | 内容 | 判定 |
| --- | --- | --- |
| `app/workflow/nodes/llm_node.py:100-101` | `api_key = os.environ.get(env_name)` | 误报：环境变量名间接引用，无明文密钥（H6/ADR-008） |
| `app/workflow/registry.py:170` | `token = set_run_collector(collector)` | 误报：contextvars token，与凭证无关 |
| `tests/unit/workflow/nodes/test_base.py:117` | 同上 contextvars token | 误报 |
| `tests/unit/workflow/test_logging_conf.py:83-84` | `sk-dummy-secret-123/456` | 误报：脱敏测试哑密钥，仅存在于断言中 |
| `tests/unit/workflow/test_cli.py:77` | `sk-live-leak-999` | 误报：H6 泄漏回归测试哑密钥，断言目标即"不得出现" |
| `tests/integration/workflow/test_exploration_llm.py`（多处） | `DUMMY_KEY` 构造参数 | 误报：EXP-L 探索记录用哑密钥，SDK 对象构造不产生网络调用 |

结论：[x] 无硬编码真实密钥。

### ② `.env` 不在仓库

```bash
ls -a . | grep -E "^\.env"   # 仅 .env.example
```

结论：[x] 仓库只含 `.env.example`，`.gitignore` 覆盖 `.env`。

### ③ DEBUG 级运行无完整 state / 密钥落日志（H6 终检）

以 `--log-level DEBUG` 运行内嵌哑密钥 `sk-live-leak-999` 的失败 workflow：

- stderr 全量日志中哑密钥命中数：**0**（`grep -c` 验证）
- 错误信封 error 字段脱敏为 `api_key=***`（redact_processor 文本替换生效）
- 日志事件仅含 `workflow_id` / `run_id` / `node` 级上下文，无完整 state 转储（S15）

结论：[x] H6 终检通过。

## 二、无界缓存终检（H4）

```bash
grep -rn "lru_cache" app/workflow/          # 零命中
grep -rnE "^_[a-z_]+ *(=|:)\s*(dict|\{)" app/workflow/   # 零命中
```

- [x] `app/workflow/` 内无 `lru_cache`、无模块级 dict 缓存。
- G7 迁移记录：spec-08 TC3 曾在 `app/workflow/api.py` 引入模块级
  `_registry_cache`，与 CONTRACT §10 G7 字面冲突；spec-09 TC1 已迁移为
  宿主组装点注入 `app.state.workflow_registry`（`app/main.py` 构建，
  `get_registry(request)` 读取；缺注入时端点返回脱敏 500 信封而非隐式重建）。
  见 commit `8a79db5`。
- 规范约束已沉淀进 README 扩展指南（H4 缓存红线：引擎模块禁止模块级无界缓存）。

## 三、构建期注册表快照终检（H5）

```bash
grep -n "registry" app/workflow/graph_builder.py
```

命中 4 处均为文档性注释与字段说明；`GraphBuilder.__init__` 明确
"deliberately no registry parameter (H5)"，`build_graph` 每次从传入的
`WorkflowDefinition` 现建节点，无任何注册表拷贝/快照。

结论：[x] H5 终检通过。

## 四、加固清单汇总

| # | 检查项 | 结果 |
| --- | --- | --- |
| 1 | 密钥硬编码 grep 三检（①） | [x] 通过（6 处误报已记录） |
| 2 | 仓库无 `.env`（②） | [x] 通过 |
| 3 | DEBUG 日志无 state/密钥泄漏（③/H6） | [x] 通过 |
| 4 | 无模块级 dict / lru_cache（④/H4/G7） | [x] 通过（含 app.state 迁移） |
| 5 | GraphBuilder 无注册表快照（⑤/H5） | [x] 通过 |
| 6 | 《安全加固清单》落盘（⑥） | [x] 本文件 |

**总体结论**：无 CRITICAL/HIGH 遗留，TC1 验收通过。
