# 05 · 契约符合性矩阵（spec-09 TC4 收官终校）

> 基准：`spec/CONTRACT.md`（最高权威）+ `00-架构总览.md` K/C + `02-开发规范.md` R
> + `03-隐患修复方案.md` H。终校日期：2026-08-11。全部条目无未决项。

## 一、§4 接口冻结签名终校

方法：AST 脚本从 CONTRACT §4 全部 `python` 代码块提取签名，与实现文件逐一比对
（`.qoder/sig_audit_spec09.py`）。结果：**checked=64，62 项逐字一致**；2 项为契约
自身已记录的修订，非偏差：

| 符号 | 契约文本 | 实现 | 依据 |
| --- | --- | --- | --- |
| `state.py::StateModelFactory.create_state_model` | `(state_schema)` | 追加 `node_names: list[str] \| None = None`（默认 None） | §4.3 修订备注【EXP-G8，2026-07-30】明示"入参形态留待 spec-02 落地"；默认值保持旧调用兼容 |
| `base.py::set_run_collector` | `-> Token` | `-> Token[RunLogCollectorLike \| None]` | 泛型参数化为 `Token` 子类型，pyright standard 要求；语义不变 |

`__version__ == "0.1.0"`（`test_import_package` 锁定），未随加固阶段变动。

## 二、K1–K10 保留项

| # | 保留项 | 落实落点 | 状态 |
| --- | --- | --- | --- |
| K1 | YAML DSL 五元组 | `models.WorkflowDefinition`（workflow_id/nodes/edges/entry_point/state_schema + operator_logs/execution_history） | [x] |
| K2 | 动态 Pydantic 状态模型 | `state.StateModelFactory.create_state_model` + `_DynamicStateBase(extra="allow")` | [x] |
| K3 | reducer 机制 | `state._build_field`：add→`Annotated[list, operator.add]`，last→`_last` | [x] |
| K4 | BaseNode 契约 | `nodes/base.py`：`build_runnable`/`validate_config`/`log_execution`/`wrap_runnable` | [x] |
| K5 | 插件式节点工厂 | `nodes/factory.py`：`register_node_type`/`list_node_types`/`create_node` | [x] |
| K6 | build_graph 七步 | `graph_builder.build_graph`（校验→state 模型→StateGraph→节点→边→入口→编译） | [x] |
| K7 | 条件边路由器 | `graph_builder._build_condition_router` 闭包 + `add_conditional_edges` | [x] |
| K8 | WorkflowRegistry 接口面 | `registry.py` 全部公开接口；`WorkflowNotFoundError`；`RunResult` | [x] |
| K9 | utils 两个状态工具 | `utils.convert_state_to_dict` / `map_output_to_state`（仅此两个公开函数，C7） | [x] |
| K10 | LLM 多供应商 / HTTP 模板化 | `llm_node`（openai/anthropic 懒加载）+ `http_node`（模板渲染/response_path） | [x] |

## 三、C1–C9 清理项

| # | 清理项 | 落实证据 | 状态 |
| --- | --- | --- | --- |
| C1 | 三职责拆分 | `state.py`/`graph_builder.py`/`registry.py` 三模块独立，均 < 400 行（R8） | [x] |
| C2 | reducer 仅 YAML 驱动 | `state.py` 零字段名特判；`grep circle_/planner_/worker_/reflector_/current_node` 零命中 | [x] |
| C3 | 路由器不 print / 无静默兜底 | structlog debug 输出；no-match 按 `no_match_policy`（raise 默认 / default 显式） | [x] |
| C4 | history 结构化增量 | `map_output_to_state` 追加 `"{node}: {output}"` 增量条目；双写文档化（README FAQ） | [x] |
| C5 | 无 dispatcher 豁免 | `_validate_definition` 干净校验；`test_validate_no_dispatcher_exemption` 守护；grep 零残留 | [x] |
| C6 | 单一 delete_workflow | `registry.delete_workflow` 三映射同删；`unregister_workflow` 已移除 | [x] |
| C7 | utils 瘦身 | 仅两个状态工具函数，无 LLM/DB 导入 | [x] |
| C8 | NodeType 精简 | 仅 `LLM`/`HTTP` 成员；别名兼容走 `create_node` 恰好 2 个内置分支（R4） | [x] |
| C9 | 密钥 env 化 + 日志脱敏 | `LLMNode._resolve_api_key`（env）+ `logging_conf.redact_processor`（H6） | [x] |

## 四、H1–H7 隐患修复

| # | 隐患 | 修复落点 | 终检证据 | 状态 |
| --- | --- | --- | --- | --- |
| H1 | Registry 并发不安全 | per-workflow `RLock` + 元数据锁 | `test_concurrent_same_workflow_logs_isolated`（16×64）+ `test_different_workflows_not_blocked`（D3，spec-09 新增）；5 轮稳定 | [x] |
| H2 | 死异常兜底 | tenacity 显式重试谓词（S8）；所有 except 有日志+重抛/分层 | llm/http 失败路径单测全覆盖（99%+） | [x] |
| H3 | 日志收集耦合 | ContextVar 运行级 `RunLogCollector`（S11） | registry/collector 单测 + 并发日志隔离断言 | [x] |
| H4 | 无界缓存 | 引擎零缓存；spec-09 迁移 `api.py` 模块级缓存至 `app.state`（G7） | `grep lru_cache` 零命中；README 扩展指南沉淀约束 | [x] |
| H5 | 构建期注册表快照 | `GraphBuilder` 无 registry 参数，每次按定义现建 | `grep registry graph_builder.py` 仅注释；spec-09 终检通过 | [x] |
| H6 | 密钥/日志卫生 | env 解析 + redact_processor + 错误信封脱敏 | DEBUG 运行零泄漏（`04-安全加固清单.md`）；`test_cli_error_no_state_leak`/`test_api_failure_envelope_redacted` | [x] |
| H7 | Registry API 不一致 | 单一 `delete_workflow` + 内部映射私有化 | C6 测试断言三映射无残留 | [x] |

## 五、R1–R10 红线

| # | 红线 | 终检证据 | 状态 |
| --- | --- | --- | --- |
| R1 | 范围纪律（仅加固不新增） | spec-09 无新节点类型/新公开接口；`get_registry` 签名变更为内部 DI 细节（不在 §4 冻结清单） | [x] |
| R2 | 禁硬编码业务字段名 | C2 grep 闸门零命中 | [x] |
| R3 | 节点进出管线 | 所有节点 `convert_state_to_dict` 进 / `map_output_to_state` 出；test_base 断言不 mutate | [x] |
| R4 | 注册式扩展 | `create_node` 恰好 2 个内置分支 + 注册表兜底（spec-04 方案 A） | [x] |
| R5 | 密钥 env / 无明文 | 密钥 grep 三检（04 清单）；`print` 仅 cli.py 豁免（T201 per-file-ignores） | [x] |
| R6 | 禁死 except | 全部 except 显式分层（llm/http/cli/api）；`BLE001` 仅 noqa 于声明的 catch-all 层 | [x] |
| R7 | 严格 TDD / 覆盖率 ≥80% | 全部任务卡 RED→GREEN；总覆盖率 99%（881 stmt，99.77%）；CI `--cov-fail-under=80` 门禁 | [x] |
| R8 | 小文件小函数 | 最大文件 llm_node.py 214 行 / http_node.py 254 行，均 < 400 | [x] |
| R9 | pydantic v2 全量标注 | pyright standard 0 errors；models 全量类型标注 | [x] |
| R10 | 缓存上限/失效/开关 | 引擎零缓存（H4 终检）；宿主 app.state 注册表生命周期与进程一致（有限、非无界） | [x] |

## 六、S1–S16 行为语义落点索引

| 语义 | 落点 | 语义 | 落点 |
| --- | --- | --- | --- |
| S1 | `state._build_field` reducer 规则 | S9 | `http_node` mock 显式开关（miss 必抛） |
| S2 | `state` history 自动注入 | S10 | `graph_builder` no-match raise 策略 |
| S3 | `utils.convert_state_to_dict` | S11 | `registry` ContextVar 运行级收集器 |
| S4 | 双写（平铺需声明 + `{node}_result`） | S12 | `registry` execution_history 仅留最近一次 |
| S5 | `llm_node` messages 进 / 输出包 | S13 | `registry.register_workflow` 原子替换 |
| S6 | `graph_builder._resolve_default_target` | S14 | LLMConfig/HTTPNodeConfig `extra="forbid"` |
| S7 | `_parse_condition`/`_resolve_path`（绝不 eval） | S15 | 全引擎 structlog 摘要日志（H6） |
| S8 | tenacity 重试谓词（429/5xx） | S16 | `models.load_definition_from_yaml` 仅 `yaml.safe_load` |

## 七、覆盖率 Miss 备注（term-missing 终态）

总覆盖率 99%（881 stmt / 2 Miss，99.77%），两处 Miss 均为不可达类型守护行，保留：

| 位置 | 内容 | 判定 |
| --- | --- | --- |
| `nodes/http_node.py:155` | `except RetryError: raise` | 不可达：`Retrying(reraise=True)` 永不抛 `RetryError`；为满足类型检查保留（注释已标明） |
| `nodes/llm_node.py:156` | 同上 | 同上 |

其余补测记录：`graph_builder` 悬空 edge source 深防御分支（spec-09 TC2）、
`__main__.py` 模块入口（runpy 测试）均已补齐至 100%。

## 八、红线 grep 机器闸门终态（CONTRACT §10）

| 闸门 | 结果 |
| --- | --- |
| C2/R2 领域字段名特判 | 零命中 |
| H4/R10 `cache\|lru_cache` | 仅说明性文字（api.py docstring）与 structlog 参数名 `cache_logger_on_first_use`，无缓存实体 |
| H1 `clear_execution_history` @ registry.py | 零命中 |
| G8/R5 `print(`（cli.py 豁免外） | 零命中 |
| S16 `yaml.load(` | 零命中（全引擎仅 `yaml.safe_load`） |
| C5 `dispatcher\|triage\|subgraph` | 零命中 |

## 九、通用 DoD G1–G8 勾选

| # | 项 | 状态 |
| --- | --- | --- |
| G1 | 公开接口类型注解完整 | [x] pyright 0 errors |
| G2 | 异常族统一（WorkflowEngineError 基类） | [x] |
| G3 | structlog 结构化日志（lowercase_underscores） | [x] |
| G4 | 测试零真实网络/LLM | [x]（真实 LLM 仅交付前人工示例验收） |
| G5 | 覆盖率 ≥80% | [x] 99% |
| G6 | conventional commits | [x] spec-09 共 4 提交 |
| G7 | 无模块级可变全局 | [x]（app.state 迁移，commit 8a79db5） |
| G8 | 机器可读输出与日志分流（CLI stdout/stderr） | [x] |

**总体结论**：K/C/H/R/S/G 全部闭环，签名终校零未记录偏差，spec-09 TC4 验收通过。
